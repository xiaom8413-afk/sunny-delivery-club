# 贡献与反馈

感谢你试驾《晴空派送局》。最有价值的反馈是：发生了什么、你原本期待什么、别人如何重现。

## 报告问题

请通过本仓库 Issues 提供：

- 游戏版本、操作系统与浏览器版本。
- 使用独立 HTML，还是源码入口 / 本地服务器。
- 所在站点、悠闲或标准模式，以及相关改装。
- 尽量简短的复现步骤，必要时附截图。
- 是否每次出现，重新开始或重新加载后是否仍出现。

不要提交浏览器登录数据、密码、Cookie 或其他私人内容。存档仅在本地；报告问题无需分享整个浏览器目录。

## 修改源码

使用 Node.js 22 或更新版本，无需安装 npm 依赖：

```sh
npm test
npm start
```

修改入口：

- 规则、关卡、碰撞、分身与存档：`engine.js`。
- 输入、绘图、音效和界面交互：`game.js`。
- 页面与样式：`index.html`、`style.css`。

请保持逻辑引擎不依赖 DOM；为影响行为的改动补充能够失败的有效测试。界面或文案调整不必添加只验证实现细节的测试。

提交前运行：

```sh
npm test
npm run build
```

将来源文件和重新生成的 `晴空派送局.html` 一起审查。不要只改独立 HTML；下一次构建会覆盖它。不要提交 `node_modules`、临时截图、个人路径或浏览器资料。

Pull request 请说明具体问题、最终行为、验证方式，以及是否改变存档或操作规则。新增玩法建议先提 Issue 描述目标，避免投入大量工作后才发现方向不一致。

## 授权说明

仓库目前未配置开源许可证。提出问题或发起 pull request 不代表作者授予商业使用或再分发许可，也不自动构成新的许可协议。商业使用、转载、分发或其他授权需求，请先在 Issues 联系作者确认。
